{
  "entities": {
    "ShareRoom": {
      "title": "Share Room",
      "description": "A temporary room for sharing documents via a 6-digit code.",
      "type": "object",
      "properties": {
        "documents": {
          "type": "array",
          "description": "List of base64 encoded documents."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the room was created."
        }
      },
      "required": ["documents", "createdAt"]
    }
  },
  "firestore": {
    "/stanze_condivisione/{roomId}": {
      "schema": "ShareRoom",
      "description": "Collection of temporary sharing rooms indexed by 6-digit codes."
    }
  }
}
